'use strict';

var _require = require('../../globals/js/settings'),
    prefix = _require.prefix;

module.exports = {
  hidden: true,
  meta: {
    useIframe: true
  },
  context: {
    prefix: prefix
  }
};